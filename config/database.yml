defaults: &defaults
  adapter: postgresql
  template: template0
  host: localhost


development:
  <<: *defaults
  database: er_share_development

test:
  <<: *defaults
  min_messages: warning #magic sauce
  database: er_share_test